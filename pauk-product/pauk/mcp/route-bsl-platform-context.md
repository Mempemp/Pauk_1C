# MCP: bsl-platform-context

Репозиторий: [mcp-bsl-platform-context](https://github.com/alkoleft/mcp-bsl-platform-context).

Читать этот файл **только** если в сессии есть инструменты этого сервера (имена смотреть в схеме MCP клиента; часто `search`, `info`, `getMember`, `getMembers`, `getConstructors`).

- Перед утверждениями о встроенном API платформы — **сначала** эти инструменты, не память модели.
- Результат MCP не подменять догадкой; при расхождении с кодом в репозитории — явно сравнить.
- Прочие MCP и приоритеты — **`pauk/mcp/README.md`**.
