# Динамические списки и условное оформление (F5–F6)

Индекс — в [SKILL.md](../SKILL.md).

## F5: динамические списки

### Почему

Динамический список — основной способ показать большие выборки с пагинацией, поиском и сортировкой. Неверная настройка даёт лишнюю нагрузку и «подвисания».

### Настройка отбора в ПриСозданииНаСервере

```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

    Список = Элементы.СписокДокументов;

    ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(
        СписокДокументов,
        "Организация",
        Организация,
        ВидСравненияКомпоновкиДанных.Равно,
        ,
        ЗначениеЗаполнено(Организация));

КонецПроцедуры
```

Имя `ОбщегоНазначенияКлиентСервер` — из типовых конфигураций; замените на проектный API.

### Произвольный запрос списка

```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

    СписокДокументов.ПроизвольныйЗапрос = Истина;
    СписокДокументов.ТекстЗапроса =
    "ВЫБРАТЬ
    |   Реализация.Ссылка,
    |   Реализация.Дата,
    |   Реализация.Номер,
    |   Реализация.Контрагент,
    |   Реализация.СуммаДокумента,
    |   ЕСТЬNULL(Задолженность.СуммаОстаток, 0) КАК ОстатокЗадолженности
    |ИЗ
    |   Документ.РеализацияТоваровУслуг КАК Реализация
    |       ЛЕВОЕ СОЕДИНЕНИЕ РегистрНакопления.ВзаиморасчетыСКонтрагентами.Остатки КАК Задолженность
    |       ПО Реализация.Контрагент = Задолженность.Контрагент
    |           И Реализация.Договор = Задолженность.Договор
    |{ГДЕ
    |   Реализация.Дата >= &ДатаНачала}";

    СписокДокументов.Параметры.УстановитьЗначениеПараметра("ДатаНачала", НачалоМесяца(ТекущаяДатаСеанса()));

КонецПроцедуры
```

### Практика

1. Не подгружать «всё сразу» вручную — динамический список сам ограничивает порции.
2. Отборы пользователя — через **компоновку** (параметры, настройки), а не жёстко зашитый `ГДЕ`, если нужна гибкость.
3. Поля сортировки — с учётом индексов в БД.
4. В произвольном запросе **не** дублировать сортировку там, где её задаёт настройка списка (избегать лишнего `УПОРЯДОЧИТЬ ПО` в тексте, если сортировка ведётся настройками списка).

---

## F6: условное оформление программно

### Почему

Программная настройка в `ПриСозданииНаСервере` (или при смене режима) даёт динамические условия и проще сопровождается, чем только «ручная» настройка в конфигураторе для сложных случаев.

### Пример: подсветка строк

```bsl
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    УстановитьУсловноеОформление();
КонецПроцедуры

&НаСервере
Процедура УстановитьУсловноеОформление()

    УсловноеОформление.Элементы.Очистить();

    ЭлементУО = УсловноеОформление.Элементы.Добавить();

    ЭлементОтбора = ЭлементУО.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
    ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ДатаОплаты");
    ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Меньше;
    ЭлементОтбора.ПравоеЗначение = ТекущаяДатаСеанса();
    ЭлементОтбора.Использование = Истина;

    ЭлементОтбора2 = ЭлементУО.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
    ЭлементОтбора2.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Оплачен");
    ЭлементОтбора2.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
    ЭлементОтбора2.ПравоеЗначение = Ложь;
    ЭлементОтбора2.Использование = Истина;

    ЭлементУО.Оформление.УстановитьЗначениеПараметра("ЦветФона", WebЦвета.МисттиРоуз);

    ПолеОформления = ЭлементУО.Поля.Элементы.Добавить();
    ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("СписокДокументов");

КонецПроцедуры
```

Имена полей (`ДатаОплаты`, `Оплачен`, `СписокДокументов`) должны соответствовать полям формы и списка.
